<!DOCTYPE html>
<html lang="<%= config.site.language %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>联系我 - <%= config.site.title %></title>
    <link rel="stylesheet" href="<%= config.site.url %>/css/style.css">
</head>
<body>
    <%- include('partials/header') %>

    <div class="container">
        <main>
            <article class="contact-content">
                <h1>联系我</h1>
                <div class="contact-body">
                    <p>如果你有任何问题或建议，欢迎通过以下方式联系我：</p>
                    <ul>
                        <% if (config.site.social && config.site.social.email) { %>
                            <li>邮箱：<a href="mailto:<%= config.site.social.email %>"><%= config.site.social.email %></a></li>
                        <% } %>
                        <% if (config.site.social && config.site.social.github) { %>
                            <li>GitHub：<a href="<%= config.site.social.github %>" target="_blank"><%= config.site.social.github %></a></li>
                        <% } %>
                        <% if (config.site.social && config.site.social.twitter) { %>
                            <li>Twitter：<a href="<%= config.site.social.twitter %>" target="_blank"><%= config.site.social.twitter %></a></li>
                        <% } %>
                    </ul>
                </div>
            </article>
        </main>

        <aside class="sidebar">
            <div class="about-widget">
                <h3>关于我</h3>
                <p><%= config.site.description %></p>
            </div>
            <div class="social-widget">
                <h3>社交媒体</h3>
                <div class="social-links">
                    <% if (config.site.social && config.site.social.github) { %>
                        <a href="<%= config.site.social.github %>" target="_blank">GitHub</a>
                    <% } %>
                    <% if (config.site.social && config.site.social.twitter) { %>
                        <a href="<%= config.site.social.twitter %>" target="_blank">Twitter</a>
                    <% } %>
                    <% if (config.site.social && config.site.social.email) { %>
                        <a href="mailto:<%= config.site.social.email %>">Email</a>
                    <% } %>
                </div>
            </div>
        </aside>
    </div>

    <%- include('partials/footer') %>
</body>
</html> 